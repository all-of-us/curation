{
    "compute":[
        {"rules":"@compute.id",
         "fields":["person_id"],
         "table":":idataset.deid_map as map_user",
         "key_field":"map_user.person_id",
         "value_field":"measurement.person_id"}],
    "shift":[
        {"rules":"@shift.date", 
         "fields":["measurement_date"]},
        {"rules":"@shift.datetime", 
         "fields":["measurement_datetime"]}
    ]
}
