{
    "compute":[
        {"rules":"@compute.id",
         "fields":["person_id"],
         "table":":idataset.deid_map as map_user",
         "key_field":"map_user.person_id",
         "value_field":"specimen.person_id"}],
    "shift":[
        {"rules":"@shift.date", 
         "fields":["specimen_date"]},
        {"rules":"@shift.datetime", 
         "fields":["specimen_datetime"]}
    ]
}

