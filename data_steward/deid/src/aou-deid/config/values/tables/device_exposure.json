{
    "compute":[
        {"rules":"@compute.id",
         "fields":["person_id"],
         "table":":idataset.deid_map as map_user",
         "key_field":"map_user.person_id",
         "value_field":"device_exposure.person_id"}],
    "shift":[
        {"rules":"@shift.date", 
         "fields":["device_exposure_start_date", "device_exposure_end_date"]},
        {"rules":"@shift.datetime", 
         "fields":["device_exposure_start_datetime", "device_exposure_end_datetime"]}
    ]
}
