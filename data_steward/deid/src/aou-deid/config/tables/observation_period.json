{
    "compute":[
        {"rules":"@compute.id",
         "fields":["person_id"],
         "table":":idataset.deid_map as map_user",
         "key_field":"map_user.person_id",
         "value_field":"observation_period.person_id"}],
    "shift":[
        {"rules":"@shift.date", 
         "fields":["observation_period_start_date", "observation_period_end_date"]}
    ]
}
